ALGORITHM insertion-sort
VAR
    T: ARRAY_OF_INTEGER;
    key,step,j:INTEGER;
BEGIN
    FOR step FROM 1 TO T.legth-1 STEP 1  DO
    key:=T[step];
    j:=step-1;
    WHILE (j>=0 AND key<=T[j]) DO
    t[j+1]=t[j];
    j:=j-1;
     END_WHILE
     T[j+1] =key;
    END_FOR
END